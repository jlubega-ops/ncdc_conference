/** Minimum share of scheduled conference days attended (0–100). */
export const CERTIFICATE_MIN_ATTENDANCE_PERCENT = 90;

/** Certificate number: NCDC/{YEAR}/{CONF}/{SEGMENT} */
export const CERTIFICATE_NUMBER_PREFIX = "NCDC";
